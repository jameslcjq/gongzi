# 待定需求

记录已经讨论过、但尚未决定的功能需求。等条件成熟再展开实现。

## 自定义字段（增删字段）

来源：2026-05-01 讨论。

### 需求

每张工作表都允许使用者自行添加/删除字段，应对实际业务里临时新增的工资项、津贴项等。

### 可行方案

- 用户的自定义字段存到独立的 `worksheet_custom_fields` 表（per-install 覆盖），不修改 `docs/data/worksheets-retained.json`（那是基线结构）
- 启动时合并：基线字段 + 用户自定义字段，调用现有的 `syncWorksheetColumns` 走 `ALTER TABLE ADD COLUMN`
- 删除走 `ALTER TABLE DROP COLUMN`（SQLite 3.35+ 原生支持）

### 待决策

1. **操作范围**：只允许"加"？还是"加 + 删 + 改名 + 改类型"都开放
2. **作用域**：所有 20 张表都允许，还是只放开纯本地表（人事系列、对照表等），锁住"导入文件驱动"的表（一体化在职、预算在职、其他人员等）
3. **字段位置**：新字段固定追加在末尾，还是允许插到任意位置（SQLite 重排列序成本高）
4. **可选类型**：文本 / 数值 / 日期 / 单选 / 多选 / 证件 — 给几种；选项类是否要带下拉值列表
5. **冲突处理**：
   - 给"导入驱动"的表加字段后，下次导入的 xls 缺这一列 → 严格签名校验会失败。要不要对自定义字段豁免签名校验？
   - 工作流（如 `更新薪级`、`计算基础性绩效工资`、`调整乡镇补贴`）依赖具体字段名（薪级、薪级工资*、岗位津贴、生活补贴、乡镇工作年限（年）、新补贴标准（元/月） 等），用户删除/改名会让规则跑挂
6. **删除策略**：硬删除（DROP COLUMN，数据丢）还是软删除（仅元数据隐藏，DB 列保留）
7. **被规则引用的字段是否锁死**：UI 禁止删/改名

### 最保守的第一版（备选）

- 只允许"加"、末尾追加、类型只有 文本 / 数值 / 日期 三种
- 自定义字段不计入导入签名校验（允许 xls 里没有这一列）
- 删除/改名做"软删除/重命名"（仅元数据层，DB 列保留）
- 工作流引用的字段在 UI 上禁动

### 影响范围

- `docs/data/worksheets-retained.json`（基线，不改）
- 新增 `worksheet_custom_fields` 表（custom 字段元数据持久化）
- `app/src/main/db/connection.ts` 的 `syncWorksheetColumns` 需读取自定义字段
- `app/src/main/db/metadata.ts` / 渲染端组件需要把基线 + 自定义合并
- `app/src/main/services/worksheetInference.ts` 的 `matchesSignature` 要把自定义字段排除（如果决定豁免签名校验）
- `WorksheetView.vue` / `RecordFormDialog.vue` 已经是按字段元数据渲染，无需大改
- 新增管理界面：字段管理 dialog（每张表的"..."菜单进入）

## 自动额度匹配：按工资批次做金额前置校验

来源：2026-06-03 讨论。

### 当前状态

暂不实现，先保持现有自动额度匹配流程。

现有本地采集/入库数据里已经有工资批次相关信息：

- `在职工资`、`退休工资`、`其他工资` 包含 `工资批次编码`
- 同一人在一体化工资数据中可能同时存在 `001` 工资批次和 `002` 数币批次
- 当前表格视图已经支持 `工资(001)` / `数币(002)` 的查看筛选

但当前自动额度匹配使用的本地汇总接口没有按工资批次拆分，只提供整体汇总值，例如 `activeOtherOneTotal`、`activeBasicPerformanceTotal`、`retiredHousingTotal`、`retiredActualPayTotal`、`otherActualPayTotal`。因此现在不适合直接拿本地总额与网页当前批次的未匹配金额做强校验。

### 需求背景

一体化额度匹配页面在点击原有“额度匹配”后，下方会显示工资项目明细。脚本目前能读取每一行的未匹配金额，但没有读取底部“工资项总额 / 已匹配金额 / 未匹配金额”的合计栏。

后续希望在自动匹配前增加一道金额校验：

1. 读取当前页面工资项目明细的未匹配金额并汇总。
2. 识别当前页面对应的工资批次，例如 `001` 或 `002`。
3. 从本地工资数据中按同一批次计算应匹配金额。
4. 页面未匹配金额与本地同批次金额不一致时，停止自动匹配并提示用户，不进入后续“修改 / 填额度 / 保存”流程。

### 关键细节

- 有数字币卡的人员，其交通费可能在 `002` 数币批次中。
- 没有数字币卡的人员，其交通费可能仍在 `001` 工资批次中。
- 因此不能简单把“交通费”固定排除或固定归入某一个批次。
- 应按本地一体化工资数据里每个人实际的 `工资批次编码` 汇总交通费。
- 如果网页当前批次无法识别，不应自动匹配，应提示“当前工资批次无法确认”。

### 后续实现方向

- 扩展 `salary-quota-match:local-summary` 或新增接口，返回按批次拆分的本地工资汇总。
- 自动额度匹配脚本在点击原有“额度匹配”并加载明细后，先识别当前页面工资批次。
- 按当前批次汇总页面未匹配金额，并与本地同批次汇总金额比较。
- 差额超过容差（建议 `0.01`）时停止。
- 在确认能稳定识别 `001` / `002` 后，再考虑让两个批次都能分别自动匹配。

### 影响范围

- `app/src/main/ipc/appApi.ts`
- `app/src/shared/types.ts`
- `app/src/main/services/monthly-payroll/monthlyPayrollDataLoaders.ts`
- `app/src/renderer/src/integration/salaryQuotaMatchScript.ts`
- `app/src/renderer/src/components/IntegratedPortalPage.vue`
